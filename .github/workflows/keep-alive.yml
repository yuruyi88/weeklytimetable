name: Keep Railway Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes
  workflow_dispatch:  # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health endpoint
        run: |
          # Replace with your Railway backend URL after first deploy
          # curl https://your-app.up.railway.app/health || true
          echo "Add your Railway URL here after first deployment"
